# Phase 5 — Content: Sections 05–07

## Goal

Populate the final three sections: Claude Code integration, Warp shortcuts/features, and power workflows. These sections introduce keyboard shortcut tables (with `<kbd>` styling) and more complex content layouts.

## Tasks

### 5.1 Section 05 — Claude Code Integration

**Badge:** workflow

Content to add:

- Intro paragraph about Claude Code as first-class terminal citizen
- "Install Claude Code" code block (npm install, verify, auth)
- "Useful aliases" code block (cc, ccr, ccfix, cctest, ccdoc)
- "CLAUDE.md — project memory" body text + code block (template)
- "Warp + Claude Code power move" — two cyan tip boxes:
  - Blocks tip (isolate Claude Code output)
  - Warp Drive tip (pin sessions as runbooks)
- Checklist (4 items: installed, aliases, CLAUDE.md, test session)

### 5.2 Section 06 — Warp-Specific Shortcuts & Features

**Badge:** power

Content to add:

- Intro paragraph about Warp's unique features
- "Navigation shortcuts" — keyboard shortcut table (7 rows with `<kbd>` keys)
- "Block navigation" — shortcut table (4 rows)
- "AI features" — shortcut table (3 rows)
- Cyan tip box about Warp Drive (⌘W)
- Checklist (3 items: split panes, Warp AI, runbook)

### 5.3 Section 07 — Power Workflows & Final Setup

**Badge:** advanced

Content to add:

- Intro paragraph about fast vs great terminal workflows
- "fzf — fuzzy find everything" code block (source, env vars)
- "Project switcher function" code block (p() function)
- "Session startup script" code block (dev-start.sh)
- "Environment & secrets management" code block (direnv)
- "Final power checklist" (5 items)
- Amber warning box about backing up ~/.zshrc

## Definition of Done

- [ ] All 3 sections have complete content matching the brief
- [ ] Keyboard shortcut tables render with styled `<kbd>` elements
- [ ] Multiple tip boxes in Section 05 display correctly
- [ ] Warning box in Section 07 uses amber styling
- [ ] All checklists have correct items
- [ ] All code blocks are accurate to the brief
